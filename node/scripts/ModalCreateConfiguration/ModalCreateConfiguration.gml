// Feather disable all

/// @param [x]
/// @param [y]

function ModalCreateConfiguration(_x = display_get_gui_width()/2, _y = display_get_gui_height()/2, _executeAfter = undefined)
{
    with(oModalParent) instance_destroy();
    
    //TODO
}