// Feather disable all

createTime = current_time;

image_xscale = (string_width( label) + 30) / sprite_get_width( sprite_index);
image_yscale = (string_height(label) + 30) / sprite_get_height(sprite_index);